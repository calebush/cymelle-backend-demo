spring:
  application:
    name: cymelle-backed
  datasource:
    url: jdbc:mysql://localhost:3306/cymelle?useSSL=false&serverTimezone=UTC
    username: root
    password: Qwerty12!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    database: mysql
server.port: 8081
